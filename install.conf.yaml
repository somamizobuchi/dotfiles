- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
      force: true
      path: .zshrc
    ~/.config/:
      force: true
      glob: true
      path: .config/*
    ~/.vimrc:
      force: true,
      path: .vimrc

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
