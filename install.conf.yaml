- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
      force: true
      path: .zshrc
    ~/.config/:
      force: true
      glob: true
      path: .config/*
    ~/.vimrc:
      force: true,
      path: .vimrc
    ~/.tmux.conf:
      force: true,
      path: .tmux.conf

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
